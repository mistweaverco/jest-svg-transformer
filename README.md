Jest SVG Transformer
====================

Transform svg files for for jest.

## Usage

Configure Jest:

```json
{
  "jest": {
    "transform": {
      "^.+\\.svg$": "@mistweaverco/jest-svg-transformer"
    }
  }
}
```

